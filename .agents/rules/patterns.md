---
trigger: always_on
---

Use modular directory management pattern for example.
lib->modules
   ->auth
     ->controllers
     ->screens
     ->widgets
     ->services
     ->models
     ->extensions

Use Getx pattern to code.